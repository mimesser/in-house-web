// TODO: find better place?

import { appColors } from './colors';
import { calcRem } from './calcRem';

export const panelBoxShadow = 'box-shadow: 0 1px 4px 0 rgba(0,0,0,0.08);';
export const appBackground = appColors.offWhite;

export const deskPad = 170;
export const deskPadRem = calcRem(deskPad);
