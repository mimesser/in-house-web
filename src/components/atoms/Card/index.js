import React from 'react';
import styled from 'styled-components';

import { panelBoxShadow, palette } from '../../../style';

export const Card = styled.section`
  display: flex;
  background-color: ${palette.white};
  cursor: pointer;
`;
