import styled from 'styled-components';

import { font, fontSize, fontWeight } from '../../../style';

export const Title = styled.div`
  font-size: ${fontSize.xLarge};
  font-family: ${font.primary};
  font-weight: ${fontWeight.bolder};
`;
